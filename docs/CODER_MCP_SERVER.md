# Coder-MCP-server (sisarrepo)

**DevWorkflow** (`dw_*`) ja **Arch MCP** (`arch_*`) ovat repossa **Coder-MCP-server** (tyypillisesti `../Coder-MCP-server` tämän repon rinnalla).

Tietopankki (`Databank/`) on tässä **datapankki-mcp** -repossa (`.gitignore`: ei committia). Mount esim. `/workspace/Databank/AI`, kun `workspaceFolder` on datapankki-mcp.
